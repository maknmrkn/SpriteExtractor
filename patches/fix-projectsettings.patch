*** a/Models/ProjectSettings.cs
--- b/Models/ProjectSettings.cs
@@
-using System;
-using System.Drawing;
-
-namespace SpriteExtractor.Models
-{
-    public class ProjectSettings
-    {
-        public string OutputFormat { get; set; } = "PNG";
-        public string OutputDirectory { get; set; } = "./Output/";
-        public bool AutoDetectEnabled { get; set; } = false;
-        // 🔧 این Property جدید برای رنگ هایلایت - حتماً باید توجه داشته باشید
-        // که System.Drawing.Color در سریال‌سازی JSON ممکن است رفتار غیرمنتظره داشته باشد
-        public Color HighlightColor { get; set; } = Color.Orange;
-    }
-}
+using System;
+using System.Drawing;
+using System.IO;
+using Newtonsoft.Json;
+
+namespace SpriteExtractor.Models
+{
+    public class ProjectSettings
+    {
+        public string OutputFormat { get; set; } = "PNG";
+
+        private string _outputDirectory = "./Output/";
+        public string OutputDirectory
+        {
+            get => _outputDirectory;
+            set
+            {
+                _outputDirectory = Path.GetFullPath(value);
+                try { Directory.CreateDirectory(_outputDirectory); } catch { /* ignore create errors here */ }
+            }
+        }
+
+        public bool AutoDetectEnabled { get; set; } = false;
+
+        [JsonIgnore]
+        public Color HighlightColor { get; set; } = Color.Orange;
+
+        [JsonProperty("HighlightColor")]
+        public int HighlightColorArgb
+        {
+            get => HighlightColor.ToArgb();
+            set => HighlightColor = Color.FromArgb(value);
+        }
+    }
+}
